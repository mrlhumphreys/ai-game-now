import type Match from '$lib/checkers/interfaces/Match';

const touchSquare = function(match: Match, playerNumber: number, touchedSquareId: number): Match {
  return match;
};

export default touchSquare 
